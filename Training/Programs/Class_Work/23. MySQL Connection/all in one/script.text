create database anikaDB;
use anikaDB;
use bank;
create table friends(
				name varchar(30), 
				phone_number bigint(10),
				address varchar(100)
			);

CREATE TABLE `anikadb`.`phone_diary` (
  `id` INT NOT NULL,
  `Person Name` VARCHAR(45) NULL,
  `Person Number` BIGINT(11) NULL,
  PRIMARY KEY (`id`));
  


insert into friends values('chittajit chakraborty', 7059663127, '24/2/12 PK road');
select * from anikadb.friends;

show databases;
use chittajit;
show tables;
select * from customers2;